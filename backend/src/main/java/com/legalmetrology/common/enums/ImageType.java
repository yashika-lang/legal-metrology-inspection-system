package com.legalmetrology.common.enums;

public enum ImageType {
    FRONT,
    BACK,
    LEFT,
    RIGHT,
    TOP,
    BOTTOM,
    /** A side photo captured outside the Guided 360deg flow, whose exact facing wasn't recorded. */
    SIDE,
    OTHER
}
